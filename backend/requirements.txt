fastapi>=0.111.0
uvicorn>=0.29.0
python-multipart>=0.0.9
pydantic>=2.7.1
mutagen>=1.47.0
requests>=2.31.0
# Pin httpx to 0.27.x — youtubesearchpython is unmaintained and breaks with
# httpx >= 0.28 due to the removed 'proxies' argument. This single pin fixes
# the "post() got an unexpected keyword argument 'proxies'" error.
httpx==0.27.0
Pillow>=10.3.0
# yt-dlp is used ONLY for metadata extraction (playlist titles, video titles).
# No media files are downloaded through it — Cobalt handles all actual downloads.
yt-dlp>=2024.1.0
youtube-search-python>=1.6.6