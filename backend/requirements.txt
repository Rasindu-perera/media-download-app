fastapi>=0.111.0
uvicorn>=0.29.0
python-multipart>=0.0.9
pydantic>=2.7.1
mutagen>=1.47.0
requests>=2.31.0
# httpx MUST stay at 0.27.x — youtubesearchpython is unmaintained and
# breaks with httpx >= 0.28 (removed 'proxies' kwarg). Do NOT upgrade.
httpx==0.27.0
Pillow>=10.3.0
youtube-search-python>=1.6.6
# pytubefix: maintained pytube fork used to extract YouTube stream URLs.
# Handles ALL YouTube & Spotify-resolved downloads (no Cobalt needed for YT).
pytubefix>=8.0.0